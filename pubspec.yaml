name: projetmobilfinal
description: "un projet qui comprendra des pages de connexion, d'inscription et d'accueil qui présentera notre todo list avec des icônes de modification et de suppression et ensuite une icône plus qui permettra d'ajouter de nouveaux articles."

publish_to: 'none' 


version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path: ^1.9.0

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  
  flutter_lints: ^5.0.0


flutter:

  
  uses-material-design: true

  
  assets:
    - assets/images/
 

  
